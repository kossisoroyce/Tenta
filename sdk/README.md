# SDK

SDK for model wrappers, drift detectors, feature connectors, policy rules, and plugins.

The SDK should provide versioned interfaces, local test harnesses, and examples for extension authors.

